<!DOCTYPE html>
<html>
  <head>
    <title>Project1</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <nav>
        <div class="logo">
          <h1>ana</h1>
          <p class="definition">noun /ˈɑːnə/ : a collection of memorable sayings, or anecdotes.</p>
        </div>
        <div class="links">
          <%= link_to 'Home', root_path %>
          <%= link_to 'Anecdotes', anecdotes_path %>
          <% if @current_user.present? %>
            <%= link_to "All users", users_path if @current_user.admin? %>
            <%# link_to "Contributions", user_path %>
            <%= link_to "Logout #{ @current_user.username }", login_path, :method => 'delete' %>
          <% else%>
            <%= link_to 'Sign up', new_user_path %>
            <%= link_to 'Sign in', login_path %>
          <% end %>
        </div>
      </nav>
      <div class="space"></div>
      <%= yield %>
    </div>  
  </body>
</html>
